version: 1
name: HelloWorld CI 1683533464822
stages:
  - name: build
    type: ci
    spec:
      steps:
        - name: Run echo
          type: script
          spec:
            run: echo "Hello Harness CI!"
